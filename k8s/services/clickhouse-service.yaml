apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  labels:
    app: clickhouse
spec:
  ports:
  - port: 8123
    name: http
    targetPort: http
  - port: 9000
    name: native
    targetPort: native
  selector:
    app: clickhouse